language: cpp

matrix:
  include:
  - os: linux
    compiler: gcc
  - os: linux
    compiler: clang

addons:
  apt:
    packages:
      - libcairo2-dev
      - libxcb1-dev

before_script:  
    - mkdir build
    - cd build
    - cmake ..

script: cmake --build .
